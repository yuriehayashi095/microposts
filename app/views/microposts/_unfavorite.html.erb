<%= form_for(current_user.favorites.find_by(favorite_id: micropost.id),
            html: { method: :delete },
            remote: true) do |f| %>

<div><%= hidden_field_tag :micropost_id, micropost.id %></div>
<%= f.submit "Unfavorite", class: "btn" %>

<% end %>